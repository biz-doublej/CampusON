## Python API (.env.example)

# Gemini AI Configuration (optional)
GEMINI_API_KEY=
GEMINI_MODEL_NAME=gemini-2.0-flash-exp
GEMINI_EMBEDDING_MODEL=text-embedding-004

# Alternative Gemini API key name (compatibility)
GOOGLE_API_KEY=

# Database Configuration
# For local dev, SQLite is fine. You may switch to Postgres/MySQL via SQLAlchemy URL.
DATABASE_URL=sqlite:///./app.db

# Application Settings
DEBUG=True
PORT=8001

# CORS Settings
ALLOWED_ORIGINS=["http://localhost:3000", "http://127.0.0.1:3000"]
ALLOWED_HOSTS=["*"]

# Poppler Path (for PDF processing) — set to your system installation
# On Windows, provide the absolute path to the Poppler bin directory
POPPLER_PATH=

# Optional: OpenAI API Key (if you plan to use OpenAI features)
OPENAI_API_KEY=
EMBEDDING_PROVIDER=gemini

# OCR (optional; set if Tesseract is not in PATH)
# TESSERACT_CMD=/usr/bin/tesseract

