events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    
    # 로그 설정
    access_log  /var/log/nginx/access.log;
    error_log   /var/log/nginx/error.log;
    
    # Gzip 압축
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
    
    # 업스트림 서버 정의 (Docker 컨테이너에서 호스트 접근)
    upstream frontend {
        server host.docker.internal:5173;
    }
    
    upstream backend {
        server host.docker.internal:8000;
    }
    
    # 메인 서버 블록 (www.kbu-ai-team.kro.kr)
    server {
        listen 80;
        server_name www.kbu-ai-team.kro.kr kbu-ai-team.kro.kr localhost;
        
        # 프론트엔드 (메인 사이트)
        location / {
            proxy_pass http://frontend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
            
            # Vite HMR 지원
            proxy_read_timeout 86400;
        }
        
        # 백엔드 API
        location /api/ {
            proxy_pass http://backend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
        
        # 인증 API
        location /auth/ {
            proxy_pass http://backend;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        
        # 문서 및 관리자
        location /docs {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        
        location /admin {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        
        # 정적 파일 캐싱
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            proxy_pass http://frontend;
            expires 1y;
            add_header Cache-Control "public, immutable";
            proxy_set_header Host $host;
        }
    }
    
    # HTTP에서 HTTPS로 리다이렉트 (SSL 설정 후 활성화)
    # server {
    #     listen 80;
    #     server_name www.kbu-ai-team.kro.kr kbu-ai-team.kro.kr;
    #     return 301 https://$server_name$request_uri;
    # }
    
    # HTTPS 서버 블록 (SSL 인증서 설정 후 활성화)
    # server {
    #     listen 443 ssl http2;
    #     server_name www.kbu-ai-team.kro.kr kbu-ai-team.kro.kr;
    #     
    #     ssl_certificate /etc/ssl/certs/kbu-ai-team.crt;
    #     ssl_certificate_key /etc/ssl/private/kbu-ai-team.key;
    #     
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
    #     ssl_prefer_server_ciphers off;
    #     
    #     # ... 위의 location 블록들과 동일
    # }
} 